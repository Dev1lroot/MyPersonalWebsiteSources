---
pagination:
  data: languages
  size: 1
  alias: lang
permalink: "{{ lang.lang }}/projects/index.html"

eleventyComputed:
  config:
    jsp: "projects"
    schema: "main"
    language:
      code: "{{ lang.code }}"
      lang: "{{ lang.lang }}"
      style: "{{ lang.style }}"
  title: "{{ locale.btn_projects[lang.code] }}"

layout: "base.njk"
---
